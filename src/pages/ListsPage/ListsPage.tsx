import { Typography } from "@mui/material";
import { ReactElement } from "react";

const ListsPage = (): ReactElement => {
    return <Typography>LISTS PAGE</Typography>;
};

export default ListsPage;
